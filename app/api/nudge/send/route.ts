// app/api/nudge/send/route.ts
import { NextResponse } from 'next/server';

// This is a placeholder API route. The logic is handled by the mock nudgeService.
export async function POST(){
  return NextResponse.json({ ok: true });
}